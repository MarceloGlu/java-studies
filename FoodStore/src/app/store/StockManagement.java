package app.store;

public class StockManagement {

	public static void main(String[] args) {

		Store basta = new Store();
		fillStore(basta);
	}
	public static void fillStore(Store store) { // podria llamarlo "basta", pero serviria solo 
												// para este store, al llamarlo store es generico
	
	
	
	
}

}
